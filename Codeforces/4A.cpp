#include<bits/stdc++.h>
typedef long long ll;
using namespace std;
#define REP(i,n) for (int i=0;i<(n);i++)
#define REP1(i,a,b) for(int i=(a);i<=(b);i++)
#define pb push_back
int main(){
    int a;
    cin>>a;
    if(!(a%2)&& a!=2) cout<<"YES";
    else cout<<"NO";
  //  system("pause");
    return 0;
}
